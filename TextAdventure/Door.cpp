#include "Door.h"
/*
void myDoor::interact()
{
	goThroughDoor();
}

void myDoor::goThroughDoor()
{
	std::cout << "You easily ";
	
	if (this->theSide == SIDE::PUSH)
	{
		std::cout << "push open the door and move into a new room. As you look around you see";
		//you go into the pull room from a push side of a door
		std::cout << pullRoom->getDescription() << std::endl;

	}
	else
	{
		std::cout << "pull open the door and move into a new room" << std::endl;
		//you go into the push room from a pull side of a door
		std::cout << pushRoom->getDescription() << std::endl;
	}
	std::cout << "as you look around you see " << std::endl;

}

void myDoor::setPushRoom(Room* thePushRoom)
{
	if (pushRoom)
	{
		Room* temp = pushRoom;
		pushRoom = thePushRoom;
		delete temp;
	}
	else
	{
		pushRoom = thePushRoom;
	}
}

void myDoor::setPullRoom(Room* thePullRoom)
{
	if (pullRoom)
	{
		Room* temp = pullRoom;
		pullRoom = thePullRoom;
		delete temp;
	}
	else
	{
		pullRoom = thePullRoom;
	}
}
*/